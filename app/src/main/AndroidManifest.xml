<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beoks.a2021knu_hackton">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.2021KNUHackton">
        <activity android:name=".ContestPackage.TeamCreateActivity" />
        <activity android:name=".ContestPackage.TeamInfoActivity" />
        <activity android:name=".ContestPackage.ContestInformationActivity" />
        <activity android:name=".ContestPackage.ContestMainActivity" />
        <activity android:name=".InfoBoard.InfoDataDescriptionActivity" />
        <activity android:name=".InfoBoard.InfoMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>